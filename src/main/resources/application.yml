spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/msv?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: 38595472
  jpa:
    hibernate:
      jdbc:
        batch_size: 1000
        order_inserts: true
        order_updates: true
      ddl-auto: create
    properties:
        show_sql: true
        format_sql: true

  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.web.servlet.error.ErrorMvcAutoConfiguration

  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 2000



logging.level:
  org.hibernate.SQL: debug




